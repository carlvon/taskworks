/**
 * Represents the possible result types of a worker execution.

 * @enum {string}
 */
export enum ResultType {
  UNKNOWN = 'UNKNOWN',
  SUCCESS = 'SUCCESS',
  FAILED = 'FAILED',
  CANCLED = 'CANCLED',
}
